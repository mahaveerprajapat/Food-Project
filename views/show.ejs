<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Web</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
<style>
  .card{
    border: none !important;
  }
 img{
  border-radius: 10rem !important;
  height: 20rem !important;
  width: 20rem !important;
 }
img:hover{
  opacity: 0.6;
}
</style>
</head>

<%- include("includes/header.ejs") %>


<body>
  <!-- <h1 class="text-center mt-4" >items details</h1> -->
  <div style="display: flex; justify-content: center; margin-top: 2rem; margin-bottom: 3rem;">

  <div class="card" style="width: 40rem;">
    <img src="<%= item.image %>" class="card-img-top" alt="...">
    <div class="card-body">
      <h5 class="card-title"><%= item.name %></h5>
      <p class="card-text"><%= item.description %></p>
    </div>
    <ul class="list-group list-group-flush">
      <li class="list-group-item">&#8377; <%= item.price %></li>
      <li class="list-group-item"><%= item.origin %></li>
      <li class="list-group-item"><%= item.category %></li>
    </ul>
    <div class="card-body d-flex">
      <form method="get" action="/items/<%= item._id %>/edit">
        <button class="btn btn-warning">Edit Food</button>
      </form>
        &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
      <form method="post" action="/items/<%= item._id %>?_method=delete">
        <button class="btn btn-danger">Delete Food</button>
      </form>
    </div>
  </div>

 
  </div>


  <%- include("includes/footer.ejs") %>

</body>
</html>